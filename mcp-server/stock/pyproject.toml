[project]
name = "stock"
version = "0.1.0"
description = "MCP server for stock VWAP analysis"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=1.27.0",
    "yfinance>=0.2.0",
]
